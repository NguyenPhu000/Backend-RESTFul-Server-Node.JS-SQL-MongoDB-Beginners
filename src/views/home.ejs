<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/styles.css" />
    <title>Home page</title>
  </head>
  <body>
    <ul>
      <div class="topnav">
        <a class="active" href="/">Home</a>
        <a href="/create">Create</a>
        <a href="/update">Update</a>
      </div>
     <div class="table-container">
      <!-- <%= JSON.stringify(listUsers) %>   JSON.stringify() dùng để hiển thị object dưới dạng string -->
      <br> 
      <span class="table-title">List User</span>
       <table>
         <tr>
           <th>Id</th>
           <th>Email</th>
           <th>Name</th>
           <th>City</th>
           <th>Action</th>
         </tr>

         
         <!-- <% for(let i = 0; i < listUsers.length; i++) { %>
          <tr>
            <td><%=listUsers[i].id  %></td>
            <td><%=listUsers[i].email  %></td>
            <td><%=listUsers[i].name  %></td>
            <td><%=listUsers[i].city  %></td>
            <td>
             <button>Edit</button>
             <button>Delete</button>
            </td>
          </tr>
          
          <% } %> -->
<!-- thay vi dung lap thong thuong thi dung lap forEach thi code se gon va tuong minh hon -->
          <% listUsers.forEach(function(user) { %>
            <tr>
              <td><%=user.id %></td>
              <td><%=user.email %></td>
              <td><%=user.name %></td>
              <td><%=user.city %></td>
              <td>
               <a href="/update/<%=user.id %>">Edit</a>
               <button>Delete</button>
              </td>
            </tr>
         <% }); %> 
       </table>
     </div>
  </body>
</html>
